apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-events
  namespace: event-manager
spec:
  selector:
    matchLabels:
      app: db-events
  template:
    metadata:
      labels:
        app: db-events
    spec:
      containers:
      - name: postgres
        image: postgres:14
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: events_db
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: password
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: db-data
      volumes:
      - name: db-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: db-events
  namespace: event-manager
spec:
  selector:
    app: db-events
  ports:
  - port: 5432
    targetPort: 5432
